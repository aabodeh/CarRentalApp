# P4 — AI dossier backfill prompt

Drafted by Claude (claude.ai chat) in [[A-006 ai log backfill prompt]].
Given to Claude Code in [[A-007 ai log backfill]].

Reproduced verbatim, including the `[PASTE P1 HERE]` / `[PASTE P3 HERE]` placeholders as they were
sent. See the note at the end of this file for how those were resolved.

This file is excluded from Prettier in `.prettierignore` — see the second note at the end.

---

delete the merge branches and:

# Backfill and reorder the AI dossier (chronological, verbatim prompts)

Work on a new branch `docs/ai-log-backfill` from main. Rewrite `docs/ai-log/` so every AI interaction
of this project so far is logged in chronological order, with the prompts copied VERBATIM from this
message. Follow the templates in `docs/templates/`. Open a PR at the end; do not merge.

## Rules
- Renumber everything chronologically: A-001 … A-007 and FL-001 … FL-003 (see lists below). Use
  `git mv` for the existing "A-001 project setup scaffolding.md" so history is kept; it becomes A-003.
- Copy prompts exactly as given below: no translating, no fixing typos, no summarizing. Where I mark
  `[…]`, the original contained a long pasted output; keep the marker and say what was pasted.
- Get dates, PR numbers and URLs from reality: `git log`, `gh pr list --state all`. All interactions
  happened on 2026-09-22 unless git says otherwise.
- The fields `verification`, `decision`, `Evaluation` and `Alternatives considered without AI` describe
  what humans did. Fill them ONLY with the facts I give you below, marked as provided by Moha. Anything
  else stays `TODO (Moha)`. Never invent them. An AI checking AI output must be labelled as
  "AI verification", never as human verification.
- Author for all entries: Moha. Chat tool: Claude (claude.ai chat, Claude Opus 5). Agentic tool:
  Claude Code (Claude Opus 5).
- Chat link for A-001, A-002, A-004 and A-006: `TODO (Moha): shared chat link`.
- Update the index in `docs/ai-log/README.md`, and fix any reference to old IDs. In particular, PR for
  `chore/expo-sdk-patch` says "A-003": update its description with `gh pr edit` to the new ID (A-005).

## Interactions (chronological)

**A-001 · Claude chat · area: docs**
Task: give Claude the course context (Drive materials), review the repo, draft the initial Claude Code setup prompt.
Prompts (Moha → Claude chat), verbatim:
1. "Acabo de iniciar el curso de Mobile Software Development en mi erasmus en la SDU. Para ello primero quiero que entiendas todo el contexto de la asignatura hasta ahora antes de ponernos manos a la obra. Para ello he puesto todos los recursos en un drive, que quiero que guardes en tu memoria para poder consultarlo siempre que sea necesario.

https://drive.google.com/drive/folders/1VK4QAcYoWvZyZG3svyiOgdb4_x9sa8HH"
2. "ahora estamos empezando en todo, el design doc está creado pero no empezado el repo está creado, es este: https://github.com/aabodeh/CarRentalApp"
3. "A mi me ha tocado la parte técnica, primero quiero que me des un prompt inicial para claude code para que configure todo lo necesario, reglas, ai policy de la asignatura, iniciar un obsidian o algo para guardar los prompts ya que el profe nos lo pidirá, el CI... Quiero que en un único prompt hacer toda esta configuración incial para que todos la tengan."
Output summary: course summary (grading, dates, AI policy, kernel NFRs K1–K3); repo review (default
AGENTS.md, no tests/CI, untyped route params, no "My bookings" screen); 10-day plan; the setup prompt P1 (below).

**A-002 · Claude chat · area: code**
Task: review Claude Code's setup plan before execution.
Prompt: "Aquí tienes el plan: […Claude Code's full setup plan pasted…]"
Output: approval with 4 changes, delivered as prompt P2 (below).

**A-003 · Claude Code (agentic) · area: code** (the existing entry, renumbered)
Task: project setup (tooling, folder skeleton, AGENTS.md, AI log vault, CI, docs). Related PR: the chore/project-setup PR.
Prompt: P1, then P2 after plan review.
Decision (provided by Moha): Modified. The plan was reviewed and 4 changes were requested before
execution (drop noUncheckedIndexedAccess, drop eslint-plugin-prettier, restrict AsyncStorage/expo-sqlite
imports in screens/components, fix this entry's area from "structure" to "code").
Verification (provided by Moha): PR reviewed and approved by a teammate before merge; CI passed.
AI verification: Claude chat independently cloned the branch, ran `npm ci` + `npm run check` (all green,
1 test passing) and inspected eslint.config.js, ci.yml, AGENTS.md.
Anything else: TODO (Moha).

**A-004 · Claude chat · area: code**
Task: review the setup result and next steps; draft the repo configuration prompt.
Prompts, verbatim:
1. "Ya ha acabado: […Claude Code's final setup report pasted…]"
2. "Vale ya he mergeado la PR, dime que le digo a claude para que haga estas cosas el si puede, luego ya hacemos lo de los logs"
Output: AI verification described in A-003; next steps (PR review by a non-author, branch protection,
log entries); prompt P3 (below).

**A-005 · Claude Code (agentic) · area: code**
Task: verify CI on main, repo settings + branch protection via gh, expo SDK patch PR. Related PR: chore/expo-sdk-patch.
Prompt: P3. Fill the output summary from this session's actual results / the PR and the protection
read back from `gh api`. Verification and decision: TODO (Moha).

**A-006 · Claude chat · area: docs**
Task: draft this log backfill prompt.
Prompt: "Vale ya ha acabado, vamos con los logs y failures, dame el prompt para que ordene todo en orden cronologico y ponga tal cual los prompts, incluye los tuyos del chat que te he puesto, ponlos todos en el prompt para que reordene"
Output: prompt P4 (this message).

**A-007 · Claude Code (agentic) · area: docs**
Task: this backfill. Prompt: P4 (copy this whole message verbatim into the entry, or into a linked file
`docs/ai-log/prompts/P4.md` if it's too long, and do the same for P1 and P3 if it helps readability).

## Failures (chronological)

**FL-001** (related A-003): Claude Code's first `jest.setup.js` mocked
`react-native/Libraries/Animated/NativeAnimatedHelper`, which doesn't exist in RN 0.86 (stale API from
training data). Caught by: the test suite failed to run. Fix: mock removed; jest-expo already mocks the
native side, so jest.setup.js stays empty.

**FL-002** (related A-002/A-003): Claude Code's plan classified the setup log entry under area
"structure", which is a design-doc plane (navigation + data model), not tooling. Caught by: plan review
(flagged by Claude chat, confirmed by Moha). Fix: changed to "code" before execution.

**FL-003 (verify first; only write it if confirmed):** AGENTS.md tells people to install dev packages with
`npx expo install <pkg> -- --save-dev`, but during setup this put packages into `dependencies`, and they
had to be moved by hand. Reproduce it on a throwaway install (then revert with git). If confirmed, write FL-003,
fix the AGENTS.md instruction to whatever actually works, and add it under "Lessons learned / known agent
mistakes" in AGENTS.md. If NOT reproducible, don't write FL-003; tell me instead.

## Verbatim prompts

### P1: initial setup prompt (A-001 output, A-003 input)
````
[PASTE P1 HERE]
````

### P2: plan review changes (A-002 output, A-003 input)
````
Plan approved with these changes:

1. Drop "noUncheckedIndexedAccess". Keep strict: true only. Six people, 10 days: the extra
   `| undefined` noise on every array/record access costs more than it catches.
2. Drop eslint-plugin-prettier. Keep eslint-config-prettier + `format:check` in CI. Formatting
   stays a separate check instead of flooding lint output and editor squiggles.
3. In the screens/components lint override, also restrict direct imports of
   `@react-native-async-storage/async-storage` and `expo-sqlite` (storage must go through repositories).
4. A-001 area should be "code" (tooling/infra), not "structure". "structure" is the design-doc
   plane (navigation model + data model).

Also add `concurrency: { group: ci-${{ github.ref }}, cancel-in-progress: true }` to ci.yml.
Proceed.
````

### P3: repo configuration prompt (A-004 output, A-005 input)
````
[PASTE P3 HERE]
````

## Finish
Run `npm run check`, commit (Conventional Commits), push, open the PR with our template
("AI used: yes — A-007"). Then list every `TODO (Moha)` field left, grouped by entry, so I can fill them in.

---

## Note on the P1 and P3 placeholders

The prompt above shipped with `[PASTE P1 HERE]` and `[PASTE P3 HERE]` unfilled — the two prompt
bodies were never pasted in. They were recovered verbatim from the Claude Code session transcripts
instead, where both appear as the original user messages that opened those sessions:

- P1 is the message that opened the [[A-003 project setup scaffolding]] session.
- P3 is the message that opened the [[A-005 repo configuration and expo patch]] session.

They are stored in [[P1]] and [[P3]]. This is the one place where the backfill used a source other
than the text of P4, and it is recorded here so the substitution is auditable rather than silent.

## Note on Prettier

The first run of `npm run format` during [[A-007 ai log backfill]] reformatted this file: it merged
the three numbered prompts of A-001 onto a single line and reflowed the fenced blocks. That silently
altered text whose entire value is being unaltered.

`docs/ai-log/prompts/` is now listed in `.prettierignore`, and these files were rewritten from source
afterwards. If you add a prompt file here, do not remove that ignore rule.

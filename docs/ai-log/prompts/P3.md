# P3 — repo configuration prompt

Drafted by Claude (claude.ai chat) in [[A-004 setup review and repo config prompt]].
Given to Claude Code in [[A-005 repo configuration and expo patch]].

Reproduced verbatim. This file is excluded from Prettier in `.prettierignore` — see the note in [[P4]].

---

The setup PR is merged. Now finish the repo configuration. Show me the exact commands before running
anything that changes GitHub settings, and wait for my OK.

## 1. Check CI on main
Use `gh run list --branch main --limit 3` to confirm the CI workflow ran on the merge commit and passed.
If it failed, stop and show me why.

## 2. Repo settings + branch protection (via gh CLI)
First check `gh auth status` and that I have admin rights on aabodeh/CarRentalApp
(`gh api repos/aabodeh/CarRentalApp --jq .permissions`). If I'm not admin, stop and give me the exact
settings to send to the owner instead.

If I am admin:
- Repo settings: squash merge only (disable merge commits and rebase merge), delete head branches
  automatically after merge.
- Classic branch protection on `main`:
  - require a pull request, 1 approving review, dismiss stale approvals on new commits
  - require status check "Lint, typecheck and test" (the CI job name), branches must be up to date
  - enforce for admins (no bypass, not even the owner)
  - no force pushes, no deletions
- Afterwards, read the protection back with `gh api` and show me the result so I can verify it.

## 3. Fix the expo-doctor warning in its own PR
- New branch `chore/expo-sdk-patch` from main.
- Run `npx expo install --check`, apply the fixes, then `npm run check` and `npx expo-doctor` (should be 21/21).
- Commit (Conventional Commits), push, and open a PR with `gh pr create` using our PR template.
  In the AI section, put "AI used: yes — log entry pending (A-003)". Don't write the log entry yet,
  I'll do the logs afterwards.
- Do not merge. A teammate reviews it.

## 4. Report back
What you changed, the protection settings as read back from GitHub, the PR link, and anything that failed.

---

## Note on the A-### reference

Section 3 asked for "log entry pending (A-003)". After the chronological renumbering in
[[A-007 ai log backfill]], the entry covering that session is [[A-005 repo configuration and expo patch]].
PR #2's description was updated accordingly with `gh pr edit`. The prompt text above is left exactly as
it was sent.
